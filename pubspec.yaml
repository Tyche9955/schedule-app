name: schedule_app
description: A beautiful schedule management app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  sqflite: ^2.3.0
  path: ^1.8.3
  intl: ^0.19.0

flutter:
  uses-material-design: true
